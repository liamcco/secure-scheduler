from setuptools import find_packages, setup

setup(
    name='schedule_lib',
    packages=find_packages(include=['schedule_lib']),
    version='0.1.0',
    description='Scheduler simulation library',
    author='Liam Cotton',
)